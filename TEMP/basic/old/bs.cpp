#include <iostream>
#include <vector>
#include <cstdio>
#include <map>
#include <deque>
#include <queue>
#include <algorithm>
#include <climits>
#include <string>
#include <set>
#include <iomanip>
// #include<bits/stdc++.h>

using namespace std;

typedef long long ll;
typedef vector<ll> vi;
typedef set<ll> si;
typedef pair<ll, ll> pi;
typedef map<ll, ll> mi;
typedef string str;
typedef deque<ll> DQ;
typedef priority_queue<ll> pDQ;

#define fi first
#define se second
#define pb push_back
#define eb emplace_back
#define ins insert
#define pbs pop_back
#define all(x) x.begin(), x.end()
#define sz(x) ((int)x.size())

#define M_ ios_base::sync_with_stdio(false);
#define T_ cin.tie(0);
#define W__ cout.tie(0);
#define mluv M_ T_ W__;

#define FOR(i, a, b) for(int i = a; i <= b; i++)
#define REP(i, a, b) for(int i = a; i >= b; i--)
#define W(i) while(i--)
#define EL cout<<'\n';

#define open(name) { \
    if (fopen(name ".inp", "r")) { \
        freopen(name ".inp", "r", stdin); \
        freopen(name ".out", "w", stdout); \
    } \
}
#define setpr(x) std::cout << std::setprecision(x) << std::fixed


const ll lmax = LLONG_MAX;
const ll lmin = LLONG_MIN;

#define add(a, b, mod) ((a) = ((a) + (b)) % (mod))
#define MX(a, b) ((a) = ((a) > (b) ? (a) : (b)))
#define MN(a, b) ((a) = ((a) < (b) ? (a) : (b)))

const int N1 = 2e5;
const int N2 = 1e6;
const ll Mod = 1e9 + 7;
const ll base = 311;
void dot(ll tesid){

}
int main() {
    mluv;
    //open("r");
    // int t;cin>>t;
    int t=1;
    FOR(_,1,t)dot(t);
    return 0;
}
